<catel:Page  xmlns:catel="http://schemas.microsoft.com/winfx/2006/xaml/presentation" x:Class="Equality.Views.AuthorizationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Equality.Views" xmlns:equality="clr-namespace:Equality" xmlns:viewmodels="clr-namespace:Equality.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:AuthorizationPageViewModel}"
             mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AuthorizationPage">
    <Page.Resources>
        <ControlTemplate x:Key="TextBoxBaseControlTemplate" TargetType="{x:Type TextBoxBase}">
            <Border Background="#FAF9FA" 
                BorderBrush="White" Margin="10,0,10,10" Padding="10,10,10,10" BorderThickness="1,1,1,1" CornerRadius="1 " HorizontalAlignment="Stretch" Cursor="IBeam" >
                <ScrollViewer x:Name="PART_ContentHost"/>
            </Border>
        </ControlTemplate>
    </Page.Resources>
        <Grid UseLayoutRounding="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Label Target="{Binding ElementName=Email}" Grid.Row="0" Grid.Column="0">Введите Email</Label>
        <TextBox Grid.Row="1" Name="Email" Grid.Column="0" Template="{StaticResource TextBoxBaseControlTemplate}"></TextBox>


        <Label Target="{Binding ElementName=Password}" Grid.Row="2" Grid.Column="0">Введите пароль:</Label>
        <TextBox Name="Password" Grid.Row="3" Grid.Column="0" Text="Test" Template="{StaticResource TextBoxBaseControlTemplate}" />
        <Button Grid.Row="4" Grid.Column="0"  Content="Войти"></Button>
    </Grid>
</catel:Page>
