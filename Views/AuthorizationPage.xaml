<catel:Page  xmlns:catel="http://schemas.microsoft.com/winfx/2006/xaml/presentation" x:Class="Equality.Views.AuthorizationPage"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Equality.Views" xmlns:equality="clr-namespace:Equality" xmlns:viewmodels="clr-namespace:Equality.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:AuthorizationPageViewModel}"
             mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AuthorizationPage"
    xmlns:converters="clr-namespace:MaterialDesignThemes.Wpf.Converters;assembly=MaterialDesignThemes.Wpf"
    xmlns:wpf="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf">
    <Page.Resources>
        <ControlTemplate x:Key="ForgotPasswordButton" TargetType="{x:Type ButtonBase}">
            <Border Background="Transparent"
                    Width="Auto"
                    Cursor="Hand">
                <ContentControl Foreground="#fe8c6d" Content="Забыли пароль?"/>
            </Border
        </ControlTemplate>
    </Page.Resources>
    <Grid UseLayoutRounding="False" Width="350">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" FontSize="27" HorizontalAlignment="Center" Margin="0,0,0,52">Вход</TextBlock>
        <StackPanel Grid.Row="1">
            <Label Margin="10,0,10,0" Target="{Binding ElementName=Email}" FontSize="16">Адрес эл. почты</Label>
            <TextBox
                Name="Email"
                Style="{StaticResource MaterialDesignFilledTextBox}"
                VerticalAlignment="Top"
                AcceptsReturn="True"
                TextWrapping="Wrap"
                materialDesign:HintAssist.Hint="Введите адрес эл. почты"
                Margin="10,0,10,25"
                FontSize="16"
                IsEnabled="{Binding Path=IsChecked, ElementName=MaterialDesignFilledTextBoxEnabledComboBox}" />
        </StackPanel>

        <StackPanel Grid.Row="2">
            <StackPanel Orientation="Horizontal" Margin="10,0,10,0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Label Target="{Binding ElementName=Password}" FontSize="16">Пароль</Label>
                <Button Template="{DynamicResource ForgotPasswordButton}" FontSize="16" Margin="144,0,0,0" ></Button>
            </StackPanel>
            <PasswordBox
                Name="Password"
                Style="{StaticResource MaterialDesignFilledPasswordBox}"
                VerticalAlignment="Top"
                IsEnabled="{Binding Path=IsChecked, ElementName=MaterialDesignFilledPasswordFieldPasswordBoxEnabledComboBox}"
                materialDesign:HintAssist.Hint="Введите пароль"
                Margin="10,0,10,25"
                FontSize="16" />
        </StackPanel>
        <Button Grid.Row="3" Margin="10,0,10,10" Grid.Column="0" Content="Войти" FontSize="16" Padding="10, 10, 10, 10" Cursor="Hand" Height="50"/>
    </Grid>
</catel:Page>
